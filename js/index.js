
// Build a component because that's what we do
class Something {
    constructor(element) {
        this.element = element;
    }
}




// Select some stuff
const iconsLeft = document.querySelectorAll('.icon-left');
iconsLeft.forEach(icon => console.log(icon));








// iconsLeft.forEach( link => new TabLink(link) );
